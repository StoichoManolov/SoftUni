num_one = int(input())
num_two = int(input())
num_three = int(input())

def add_and_subtract(one, two, three):
    sum_of_numbers = one + two
    subtract_of_numbers = sum_of_numbers - three
    return subtract_of_numbers

print(add_and_subtract(num_one, num_two, num_three))


